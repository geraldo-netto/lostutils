# TODO

## Open

| id | status | severity | effort | description |
|---|---|---|---|---|
| bt-watch-70 | open | high | low | bookmark-tidy.py:1120 — [watchdog] `--auto-install-llama` runs `pip install` inside the worker, so the install (own 300s timeout, source builds take longer) is charged against the parent's 300s `LLAMA_MODEL_LOAD_TIMEOUT_SECONDS` wait at line 1188; the parent then `_abort()`s the worker mid-install, reports `could not load model: timed out`, and the pip grandchild keeps running orphaned (reproduced: `sleep 30` stand-in still alive after abort). Install in the parent before spawning, or kill the worker's process group. |
| bt-cfg-70 | open | medium | medium | bookmark-tidy.py:1219-1227 — [configuration discoverability] `LLAMA_INFERENCE_TIMEOUT_SECONDS = 120` (line 36) has no CLI flag unlike every other LLM knob, and one timeout `_abort()`s the worker for good; the next two batches fail instantly on the closed pipe, the 3-failure limit trips, every remaining bookmark gets the fallback category and `_run` still exits 0 (reproduced with a 1s timeout: all folders `Uncategorized`). Expose `--llm-inference-timeout`, restart the worker instead of counting a dead one as two failures, and exit non-zero when categorization was aborted. |
| bt-plat-70 | open | low | low | bookmark-tidy.py:141-146 — [platform] Netscape root detection relies solely on English folder names in `ROOT_ALIASES` and ignores the standard `PERSONAL_TOOLBAR_FOLDER`/`UNFILED_BOOKMARKS_FOLDER` H3 attributes (captured in `handle_starttag` at line 211 but discarded), so Edge ("Favorites bar") and localized exports land under `bookmark_bar` and `--immutable-root "Other Bookmarks"` silently protects nothing. Honour the marker attributes before the name aliases. |
| bt-rob-70 | open | low | low | bookmark-tidy.py:1609 — [robustness / recovery] `_atomic_write_text` keeps `mkstemp`'s 0600 mode through `os.replace`, so output is always 0600 regardless of umask and `--force` over an existing 0664 file silently drops group/world readability (reproduced). `chmod` the temp file to `0o666 & ~umask` or copy the target's mode before replacing. |
| bt-rel-71 | open | low | low | bookmark-tidy.py:1647 — [reliability / correctness] `load_immutable_file` wraps only `OSError`; a non-UTF-8 `--immutable-file` raises `UnicodeDecodeError`, which `main`'s except clause does not catch, so the CLI dies with a traceback instead of an `error:` line (reproduced with `Work\xff`). Catch `UnicodeDecodeError` and raise `UserError`, or reuse `_read_utf8_text`. |
| bt-prod-71 | open | medium | low | bookmark-tidy.py:1846-1862 — [product engineering] The "output exists, pass --force" check runs only in `write_output` (line 1596) after `tidy_bookmarks` has completed the full LLM categorization, so a minutes-to-hours run is discarded and must be repeated (reproduced: `constructed`, `inference`, `closed`, then `UserError`). Validate output path/`--force` and `--model` existence before starting categorization. |
| bt-rel-70 | open | medium | low | bookmark-tidy.py:280-282 — [reliability / correctness] `chromium_json_data_to_bookmarks` assumes every `roots` value is a mapping; older Chrome `Bookmarks` files carry `"sync_transaction_version": "1"` inside `roots`, so `node.get("children")` raises `AttributeError` and the whole file is discarded with only a warning (reproduced: 0 bookmarks). Skip non-Mapping root values, mirroring the child check at line 283. |
| bt-ux-70 | open | low | low | bookmark-tidy.py:731 — [UI / UX] An explicitly named input with an unsupported suffix (`Bookmarks.bak`, `places.sqlite.backup`) is dropped without any message; alone it yields the misleading `no bookmark inputs found` although the path exists (reproduced). Content-sniff or warn "unsupported bookmark file" for explicit paths; keep the suffix filter for directory scans only. |
| bt-prod-70 | open | low | low | bookmark-tidy.py:758-766 — [product engineering] Auto-discovery globs only `~/.mozilla/firefox` and `~/.config/{google-chrome,chromium,microsoft-edge}`; snap Firefox (`~/snap/firefox/common/.mozilla/firefox`) and Flatpak browsers (`~/.var/app/<id>/`) on stock Ubuntu are missed, and Windows lacks `Chromium\User Data`, so the documented no-argument mode reports `no bookmark inputs found`. Add snap/flatpak/Chromium pattern roots. |
| bt-url-70 | open | medium | low | bookmark-tidy.py:836-844 — [CLI / option integrity] `--keep-fragments` help says fragments matter only "when matching duplicates", but `_normalized_display_url` is what gets exported, so `https://app.example.com/#/settings/profile` is rewritten to `https://app.example.com` (hash-routed SPA bookmark destroyed) and anchors are lost (reproduced via `tidy_bookmarks`). Export the preferred member's original URL, normalizing for keying only, or document that URLs are rewritten. |
| bt-di-70 | open | medium | low | bookmark-tidy.py:956 — [data integrity] `_merge_bookmark_group` picks the title by raw length, and plain-text inputs set `title=url`, so a URL-as-title from a `.txt` list beats the real browser title whenever the URL is longer (reproduced: merged title became the URL). Rank titles equal to the bookmark URL below any human title before comparing length. |
| dnp-di-70 | open | high | medium | dedupl_numpy.py:104 — [data integrity] Reject the producer's `<digest> +N more` alias-cap sentinel (hash-recursive-ai5.py:1314, and `+N more (alias-cap)` at :2116) instead of emitting `+2 more` as a path; the README `xargs -0 rm -f --` pipeline then tries to delete that relative name from cwd. Reproduced end-to-end with `--alias-cap 2`. Producer should render the sentinel as a `#` comment or tagged form consumers skip. |
| dnp-val-70 | open | low | low | dedupl_numpy.py:132 — [input validation / command safety] `_decode_record_path` catches only `JSONDecodeError`/`ValueError`; a `@lostutils-json:[[[[...` payload with 100k brackets raises `RecursionError` and crashes with a traceback instead of counting a skipped record (same at remove-deduplv3.py:78). Add `RecursionError` to the except clause or bound payload length. |
| dnp-val-60 | open | medium | low | dedupl_numpy.py:140 — [input validation / command safety] Reject empty paths, embedded NULs, and unencodable JSON surrogates before emitting decoded paths; `"first\u0000second"` produces two operands under `--print0`, `"\ud800"` raises an uncaught UnicodeEncodeError, and a CRLF record with an empty path is accepted. Validate decoded and raw paths and report malformed records consistently. |
| dnp-obs-71 | open | low | low | dedupl_numpy.py:163-165 — [observability / operability] In line mode a raw path containing `\r` is flagged `unprintable` and "kept escaped" via `path = raw`, but `raw` is the same bytes, so the CR is written anyway while stderr claims it was left escaped. Only fall back to `raw` for tagged forms; otherwise drop the record and report it as malformed. |
| dnp-rel-70 | open | high | low | dedupl_numpy.py:44-48 — [reliability / correctness] `_record_layout` sets `path_offset = first_space + 1`, but hash-recursive-ai5.py's `--hashes-file` dump pads the digest with `ljust(158)`, so every emitted path carries the padding (reproduced: 94 leading spaces per path; `--print0 \| xargs -0 rm` targets nonexistent names). Locate the path start as the first non-space byte after the digest. |
| dnp-ux-70 | open | low | low | dedupl_numpy.py:51-52 — [UI / UX] A single empty line (e.g. trailing `\n\n` after concatenating hash files) aborts the run with `error: every record must contain a hash and nonempty path` and no line number, whereas remove-deduplv3.py skips and counts such lines. Skip empty records and report the first offending 1-based line in every layout error. |
| dnp-obs-70 | open | low | low | dedupl_numpy.py:64-65 — [observability / operability] `_open_input` returns `None` when `st_size == 0`, which `fstat` also reports for a pipe, so `cat hashes.txt \| dedupl_numpy.py /dev/stdin` exits 0 with no output and no message. Check `stat.S_ISREG` and fail with a clear error for non-regular input, and print the `equal files: 0 / 0` summary for a genuinely empty file. |
| dnv3-rel-70 | open | low | low | deduplicate-by-namev3.py:201 — [reliability / correctness] Input is opened as `utf-8`, not `utf-8-sig`, and `strip()` keeps U+FEFF, so a BOM-prefixed file reports `<BOM>abc;abc;1` as a distance-1 pair instead of the distance-0 collision; remove-deduplv3.py already BOM-detects, so the two disagree. Open with `utf-8-sig` (still `surrogateescape`). |
| dnv3-api-60 | open | medium | low | deduplicate-by-namev3.py:219 — [API contract & compatibility] Make data records distinguishable from comment records when a cleaned input starts with `#`; input lines `#abc` and `#abd` emit `#abc;#abd;1`, which consumers following the documented instruction to skip `#` lines discard. Define escaping or an unambiguous comment marker and cover both emitters. |
| dnv3-rob-60 | open | low | low | deduplicate-by-namev3.py:235 — [robustness / recovery] Flush output inside the BrokenPipeError guard and silence stdout after an early reader close; a real subprocess whose reader closes after one byte currently exits 120 with `Exception ignored while flushing sys.stdout`. `_emit_results` returns False but main ignores it. |
| hr-api-70 | open | medium | low | hash-recursive-ai5.py:1314 — [API contract & compatibility] The alias-cap `+N more` sentinel is emitted at 1374 as a normal `<digest> <path>` record (`_encode_record_path` does not escape it), and `HashDumpWriter.write_overflow` writes `+N more (alias-cap)` into the hashes dump, so documented consumers parse it as a path (see rdv3-di-70, dnp-di-70). Emit the sentinel as a `#` comment or via the tagged-JSON escape and add a consumer-side regression test. |
| hr-cli-70 | open | low | low | hash-recursive-ai5.py:2185 — [CLI / option integrity] `--help` describes a "two-stage hash" while the pipeline has three stages (`hashed_stage3` in the summary), and `--quiet` help at 2192-2195 promises to suppress run warnings while `_emit_run_warnings` at 2222-2223 prints the SIGINT partial-results warning unconditionally (traced). Say "three-stage (head, sampled, full-file)" and honour or document the exception. |
| hr-obs-70 | open | low | low | hash-recursive-ai5.py:2414 — [observability / operability] The stall monitor's hash liveness is touched only when a whole batch future completes (`_StageProgress.__call__`), so a healthy 64-file stage-3 batch on a spinning disk triggers a false `no hash progress ... I/O may be stalled` warning every 60 s, printed even under `--quiet` (reproduced: 6-item batch at 0.5 s/item with a 1 s threshold logged three warnings). Touch the monitor per hashed item or per read chunk in `_read_window_into` (571-586). |
| hr-rob-70 | open | low | low | hash-recursive-ai5.py:2522 — [robustness / recovery] Nothing handles `BrokenPipeError` from the streaming stdout writer, so `hash-recursive-ai5.py DIR \| head -n 1` ends with a traceback, `Exception ignored while flushing sys.stdout`, and exit 120 (reproduced with 3000 pairs). Catch it around the pipeline in `main`, dup `/dev/null` onto stdout's fd as remove-deduplv3.py's `_silence_stdout_after_broken_pipe` does, and return a defined exit code. |
| hr-perf-70 | open | medium | medium | hash-recursive-ai5.py:65 — [performance] `HASH_BATCH = 64` batches are fixed by item count and one batch is one future (`_iter_batches` 933-945, `_make_full_stage3_batch` 916-930), so effective parallelism is `ceil(items/64)`, not `--jobs`; a handful of large duplicate ISOs hashes fully serial while other workers idle (reproduced: 8 identical 8 MiB files with `jobs=8` ran every `hash_full` on one thread). Cap batches by cumulative bytes or submit stage-2/3 items individually. |
| hr-sec-70 | open | high | low | hash-recursive-ai5.py:692-696 — [security] STRIDE Denial of service: the walk admits only `S_ISREG`, but `_open_hash_file` uses `O_RDONLY\|O_NOFOLLOW\|O_CLOEXEC` without `O_NONBLOCK` and no type check before reading, so a path swapped for a FIFO between walk and hash (any owner of a directory inside the tree) blocks the worker forever and the run hangs; the identity fstat at 716 runs after the open. Reproduced: `hash_head` on a swapped FIFO never returned. Add `getattr(os, "O_NONBLOCK", 0)` to `_hash_open_flags` and reject non-regular `initial.st_mode` via the existing EBUSY path. |
| hr-watch-70 | open | medium | low | hash-recursive-ai5.py:916-930 — [watchdog] `cancel_event` is consulted only between batches (1001, 1016); `_head_batch`, `_tail_batch`, and `_full_batch` loop over 64 files without checking it, and up to `jobs*SUBMIT_WINDOW` batches are in flight, so after Ctrl-C the run keeps full-file hashing up to 1024 files and `ThreadPoolExecutor.__exit__` waits for them (reproduced: 127 of 129 remaining files hashed after cancel). Check `_cancelled(cancel_event)` per item inside the closures and ideally per chunk in `_read_window_into`. |
| ie-watch-70 | open | medium | medium | import_events.py:1241 — [watchdog] The download stall guard runs only after `response.read(1 MiB)` returns, but `HTTPResponse.read(n)` blocks until all n bytes arrive, so a trickle slower than 1 MiB per 300s yet faster than the 30s socket timeout is detected only per whole chunk, and the chunk received during the stall is discarded before `handle.write` (reproduced: 1 byte/0.2s server, `.part` held 0 bytes). Use `read1`/small raw reads or a timer deadline, and write the received chunk before raising. |
| ie-dep-70 | open | medium | medium | import_events.py:1546 — [dependability] `get_llm` runs `ensure_models_exist` (multi-GB download plus SHA verification) before `from llama_cpp import Llama` at line 1560, and the ImportError is caught per file by `_llm_response_text` (line 3395), so every file re-enters `get_llm`, logs its own traceback, and the run exits 1 instead of 2 (reproduced: 3 files, 4 download attempts). Probe the import first and convert bootstrap failures into `ModelUnavailableError`. |
| ie-rel-70 | open | high | low | import_events.py:2582 — [reliability / correctness] `_read_text` reads `max_chars*4` bytes then strict-decodes UTF-8; when the cut lands inside a multi-byte sequence (any non-ASCII file above the window) decode fails and the whole window goes to the charset sniffer, which mis-identifies clean UTF-8 (reproduced: Portuguese text came back as cp1250 mojibake, a `€`-only file as cp037). Use an incremental UTF-8 decoder or strip the trailing incomplete sequence before sniffing. |
| ie-di-70 | open | high | low | import_events.py:2679 — [data integrity] `_stage_cache_entries` selects every `*.json` in `--stage-cache-dir`, and both `_prune_stage_cache` and `--reset-stage-cache` delete them, so pointing the cache at a directory holding other JSON (e.g. `.` with `events.json`) destroys user data (reproduced: `events.json` unlinked by prune and by reset). `tests/test_import_events.py:2258` codifies deleting a foreign `one.json`. Only treat `^[0-9a-f]{64}\.json$` entries as cache files. |
| ie-cache-70 | open | medium | low | import_events.py:3726 — [caching strategy] `_pdf_ocr_text` writes the `_safe_pdf_stage` fallback `""` to the `pdf_ocr` stage cache even when the render/OCR stage raised, so a transient failure is persisted as a successful empty result and later runs with `--stage-cache on` never retry OCR (reproduced: second call with a working renderer returned `""` from cache). Skip the cache write when `stage_failures` grew during the stage. |
| ie-api-70 | open | low | low | import_events.py:4628 — [API contract & compatibility] `_add_ics_end` rejects only an end strictly before the start, so an all-day event with `end == start` emits `DTEND;VALUE=DATE` equal to `DTSTART`, which RFC 5545 §3.6.1 forbids (exclusive end); strict importers reject the VEVENT or render a zero-length event (reproduced with `build_ics`). Omit DTEND or add one day when a DATE end equals the start. |
| ie-cli-70 | open | low | low | import_events.py:4917 — [CLI / option integrity] `_prepare_output_parents` and `_outputs_are_distinct` apply `expanduser()` to `--output`/`--emit-ics`, but `write_events_json(events, Path(args.output))` at line 4902 and `_output_lock` do not, so `--output=~/events.json` validates against `$HOME` then crashes with `FileNotFoundError: '~/events.json.lock.guard'` (reproduced, exit 1). Expand once when resolving args and reuse that path everywhere. |
| ie-api-71 | open | medium | low | import_events.py:4985 — [API contract & compatibility] The documented exit contract (1 = some files failed, 2 = setup error) is broken by setup errors escaping `_run_main` as raw tracebacks with exit 1: an output lock held by another run (`FileExistsError` from `_output_lock` in `write_events_json` at line 4902) and an unreadable input directory (`PermissionError` via `_run_file_workers`), both reproduced. Catch `OSError` around `process_folder` and the output writes and return 2 like `_prepare_output_parents`. |
| lq-sec-70 | open | medium | medium | link_queue.py:246 — [security] STRIDE Tampering → Elevation of privilege: when the per-user dir has no state file, `_resolve_state_path` adopts the script-dir `link_queue_state.yaml` verbatim, including each entry's `template` and `shell: true`, and `_restore_queue_from_state` (1663) executes it at startup with no confirmation; `STATE_FILE` then stays in the script dir permanently. Reproduced: planted `template: "true {url_quoted}; touch MARKER", shell: true` created MARKER after restore plus one worker. Honour the legacy file only when the script dir is user-owned and not group/world-writable, or migrate it once into the 0700 user dir as lq-sec-02 did for config. |
| lq-obs-70 | open | low | low | link_queue.py:3432 — [observability / operability] When `_open_locked` fails, `LogSink._flush_batch` discards the dequeued batch, yet the one-time warning at 3517-3522 promises lines "will continue to be queued"; later batches are dropped silently and uncounted (reproduced: only the line written after the dir appeared survived). Retain the batch bounded until open succeeds, or count drops and reword the message. |
| lq-sm-70 | open | medium | low | link_queue.py:4921 — [state machine integrity] A worker-thread `_refresh_queue_list()` sets `_refresh_pending = True`, then `_safe_after` drops the job when `_tk_jobs` is full (line 5293), but nothing resets the flag, so every later refresh including main-thread Remove Selected / Clear Queue returns early and the Treeview stays frozen for the session (reproduced with a stub). Have `_safe_after` report whether it enqueued and clear the flag on failure, or reset it in `_poll_tk_jobs` when drops occurred. |
| lq-di-70 | open | high | low | link_queue.py:5642-5736 — [data integrity] `_save_config` only sets `_settings_dirty` while the non-modal Settings dialog is open (deferral at 3948) and the flush lives solely in `_on_close_settings_dialog`; `_shutdown`/`_on_close` never flush it, so closing the main window with Settings open discards every edit despite the dialog's "Changes apply immediately" text (reproduced under Xvfb: added protocol missing from config). Call `_write_config_now()` in `_shutdown` when dirty, before the state save, and add a close-with-dialog-open test. |
| lq-val-70 | open | low | low | link_queue.py:5958 — [input validation / command safety] `MappingEditor._on_save` accepts any non-empty flag, but `_extra_argv`/`_extra_shell` lex it with `_split_command_template` at run time, so a flag with an unbalanced quote (`-o "`) saves fine and every item using that prefix then fails with `No closing quotation` and renders as `(invalid template)`; `ProtocolEditor._validate` at 5860 already rejects the same input. Run `_split_command_template(flag)` in the mapping editor and show the same error dialog. |
| lq-ux-70 | open | low | low | link_queue.py:6016 — [UI / UX] A second launch against a locked state file prints the `StateFileLockError` explanation to stderr and exits 1 after destroying the Tk root, so a user starting from a desktop launcher sees nothing happen and never learns the stale-lock remedy (traced). Show the text in a `messagebox.showerror` guarded against `TclError` before exiting. Laws of UX: visibility of system status. |
| lq-rob-70 | open | medium | low | link_queue.py:932 — [robustness / recovery] A YAML parse error in the user config yields `(None, False)` with a stderr-only warning (invisible under a desktop launcher), the app runs on defaults, and the next `save()` at 916 overwrites the file with defaults, destroying every custom protocol template with no backup (reproduced: `yt-dlp` template gone after one save). Rename the unparsable file to `<name>.corrupt-<ts>` or refuse to save while a load-failed flag is set, and surface the failure in the GUI log. |
| mkp-watch-70 | open | medium | low | minikeypad.py:156 — [watchdog] `PROBE_TIMEOUT_MS` covers `connect()` plus `_version_check` (lines 380-393, 1785-1799), but the version check alone takes 3 rids × (3 × 500 ms + 150 ms backoff) = 4.95 s when a device NAKs every probe report (measured), leaving 50 ms for enumeration and claim; a slow keypad is declared "probe stalled", abandoned to the reaper, reconnected next poll, and flaps "USB unresponsive" every ~6 s instead of the intended "defaulting to 0" fallback. The autouse `_no_sleep` fixture hides this. Size the probe deadline from the retry constants or exclude the version check from it. |
| mkp-ux-70 | open | low | low | minikeypad.py:1822-1829 — [UI / UX] `_flash_status` schedules an unconditional clear 2.5 s after each call and never cancels the previous timer, so "Write success" shown 2 s after "Busy, try again" is blanked 0.5 s later by the stale timer (traced). Keep the `after` id and `after_cancel` it before rescheduling. Laws of UX: Doherty Threshold (feedback must persist long enough to be read). |
| mkp-sm-70 | open | low | low | minikeypad.py:2040-2047 — [state machine integrity] `_precheck_write` runs before the modal `askyesno` at 2092-2101, and Tk keeps servicing `after` callbacks inside the nested loop, so the 1 s poller can take `io_busy` for a liveness probe or drop the device while the dialog is up; "Yes" then calls `_spawn_io_worker` on top of the probe's ownership, and `_probe_settled` clears `io_busy` mid write-all, or write-all runs against a dropped device reporting "0/N written" (traced). Re-run `_precheck_write` after the dialog returns. |
| mkp-prod-70 | open | low | low | minikeypad.py:2184 — [product engineering] `App()` is constructed outside any handler, so running without a display (SSH, headless CI, `DISPLAY` unset) prints a raw `_tkinter.TclError: couldn't connect to display ""` traceback (reproduced with `DISPLAY= python minikeypad.py --no-auto-install`). Catch `tk.TclError` around `App()`, log a one-line "no display available" hint, and exit non-zero. |
| mkp-obs-70 | open | medium | low | minikeypad.py:315-317 — [observability / operability] `_report_missing_backend` dedupes only `NoBackendError`; a device present but not permitted (Linux without the udev rule, macOS kernel-owned HID) makes `is_kernel_driver_active` and `get_active_configuration` raise `USBError [Errno 13]` on every 1 s poll, logging two lines forever (reproduced: 5 polls, 10 lines), so the 1000-line GUI log cap scrolls away every other message within ~8 min. Dedupe by error text and log on change, like the backend case at lines 275-278. |
| mkp-di-70 | open | medium | medium | minikeypad.py:509-514 — [data integrity] A modifier clicked after a key is written into the next empty group's modifier byte (pointer already advanced), so `_kbd_reports` never emits it, yet `fun_text()` lists it and `_download` stores `desc="A Ctrl"` while the device is programmed with plain `A` (reproduced: `basic_key(4,"A"); basic_modifier(1,"Ctrl")` reports modifier byte 0; same on the Fun tab via `fun_modifier` at 521-524). Apply a trailing modifier to the last written group or refuse it, and derive `desc` from the bytes actually sent. |
| mkp-rel-70 | open | medium | medium | minikeypad.py:531-542 — [reliability / correctness] On the Ctrl-Shift-Alt tab a modifier followed by Shift+symbol is lost: `shift_and` bumps `kc += 2` past the pending modifier, storing Shift+symbol in group 2, but `_general_char_set` counts only 1 group, so the device receives only `(Ctrl, 0)` while the display reads "! Ctrl" (reproduced; neither order yields Ctrl+Shift+symbol). Merge the Shift bit into the pending modifier group or count the skipped slot. Tests at `tests/test_minikeypad.py:192-206` cover only the no-modifier cases. |
| oze-rob-70 | open | medium | medium | organize_by_extension.py:1330-1339 — [robustness / recovery] A SIGKILL after `_reserve_target` but before `os.replace` leaves a 0-byte placeholder under the real name plus a `.<name>.<hex>.tmp` partial copy that nothing recognises on re-run (reproduced: exit 0 with `big.bin` as an empty fake in one bucket, the real file in the next, and the temp relocated to `tmp/` as user data). The `_move_cross_device` docstring at 1813-1825 covers only exceptions. Reserve by no-clobber `os.link(tmp, target)` and skip or clean `.<name>.<hex>.tmp` siblings during the scan. |
| oze-rob-71 | open | medium | medium | organize_by_extension.py:1416-1420 — [robustness / recovery] The same-fs `move_file` link path has the same non-crash-atomic window between `os.link` and `os.unlink(source)` as the cross-device path but no idempotent recovery: on re-run the planner sees the name taken and silently allocates a second bucket, leaving the file hardlinked in two buckets with exit 0 (reproduced: `txt/x00000/x.txt` and `txt/x00001/x.txt`). On a name clash check inode equality via `os.path.samefile` and finish the interrupted move by unlinking the source. |
| oze-rel-71 | open | medium | low | organize_by_extension.py:1651 — [reliability / correctness] Renaming a blocker to `<name>.collision<n>` (also 1586) gives it the declared extension `collisionN` per `normalize_extension` (106), so run 1 buckets it under the pre-rename extension and run 2 re-buckets it under `collision1/` with no tree changes (reproduced with a file named `txt` beside `note.txt`). Strip a trailing `.collision\d+` in `normalize_extension` or use a suffix `Path.suffix` does not see. |
| oze-sec-70 | open | high | low | organize_by_extension.py:1669 — [security] STRIDE Tampering: `_find_destination_blocker` (1493-1498) refuses to follow a symlink blocker, but `_reserve_collision_candidate` then renames it with plain `os.link(source, candidate)`, which follows the link, destroying it and replacing it with a hardlink to its target that the next run buckets into `collision1/` (reproduced: `root/pdf -> /outside/secret.txt` became `pdf.collision1` sharing the secret's inode). Mirror the planning-pass non-`S_ISREG` guard in `_resolve_source_collision` and use `follow_symlinks=False` or `os.rename` for symlink blockers. |
| oze-watch-70 | open | medium | medium | organize_by_extension.py:2444 — [watchdog] `_wait_for_move_futures` detects only "no completed future for 300 s" (constant at 63, no override), not progress, so one legitimately long move (multi-GB file onto a FAT/exFAT drive via the copy path) aborts the run with `MoveStallError`, `executor.shutdown(wait=True)` then blocks until the copy finishes anyway, and that completed move is never drained so `processed` under-reports (reproduced with scaled timings). Key the watchdog on bytes copied or scale the deadline by in-flight size, and expose the threshold. |
| oze-rel-70 | open | medium | low | organize_by_extension.py:256-278 — [reliability / correctness] `CONTAINER_FAMILIES` lacks families for several sniffed headers, so correctly named files of a distinct standard format are relocated as "mismatches" with no opt-out short of `--no-sniff` (reproduced: `clip.webm` → `mkv/`, `voice.opus` → `ogg/`, `lib.so` → `elf/`, `photo.dng` → `tiff/`); zip/rar/ole2/mp4/mp3/gz already have families. Add families for mkv, ogg, elf, tiff, macho, sqlite, png, jpg and generalise `--extra-zip-family` to `--extra-family <label>:<ext,...>`. |
| oze-cli-70 | open | low | low | organize_by_extension.py:919-931 — [CLI / option integrity] `--preview` is documented as "Show planned moves without applying them" and the code asserts preview never touches the filesystem, yet `_probe_case_folding` (via `is_bucketed_file` 677) creates and deletes `OzeCase<hex>.probe` in the root, mutating its mtime and firing inotify watchers (reproduced by spying `O_CREAT`). In preview, use the host default or probe under `tempfile.gettempdir()` on the same device and log the assumption. |
| lq-doc-70 | open | low | low | README.md:5 — [documentation] States `link_queue.py` "opens its GUI immediately" and lacks `--help`, but `build_parser` at link_queue.py:5975 now handles `--help`/`--version` and exits without a GUI (verified). Update the sentence to match. |
| dnp-doc-60 | open | high | low | README.md:64 — [documentation] Replace the destructive `dedupl_numpy.py --print0` example with a non-destructive consumer or explicitly explain that it deletes every copy; `group_duplicates` selects all members of each repeated-hash group, so the documented `xargs -0 rm -f --` pipeline preserves no survivor. Also correct line 59: hash width is inferred, not fixed at 32 bytes. |
| rf-rel-70 | open | medium | low | relocate_folder.py:1004 — [reliability / correctness] The pinned copy path sizes the disk-space precheck by `st_blocks * 512` (also 1073) on the premise that the copy preserves holes, but `_copy_pinned_regular` (994) streams with `shutil.copyfileobj`, which writes every zero (reproduced: 512 MiB sparse source, precheck demanded 0 bytes, destination allocated 512 MiB), so a sparse-heavy tree passes the precheck then hits ENOSPC. Size by `max(apparent, allocated)` or make the copy hole-preserving (SEEK_HOLE/SEEK_DATA or `os.copy_file_range`). |
| rf-rob-70 | open | medium | medium | relocate_folder.py:1076 — [robustness / recovery] `_CopyTargetOwner.create()` renames the empty staging dir to the final target name before `_populate_pinned_copy` writes a byte, and no SIGTERM handler is installed, so abrupt death leaves a half-written tree at `<dest_root>/<name>` indistinguishable from a complete-but-unswapped copy (reproduced: SIGKILL left a truncated file, next run exits 1 with "may be a stale partial target" while line 2954 falsely logs "any partial target was cleaned up"). Populate inside a private `.relocate-copy-*` envelope and publish on success as the legacy path at 794 does, or write a completion marker; drop the misleading hint. |
| rf-legacy-70 | open | medium | high | relocate_folder.py:1215 — [legacy / deprecation] `execute` → `_execute_migration` always passes a live `source_fd`, so the entire `shutil.copytree` implementation (`_copy_tree_unpublished`, `_copy_staging_envelope`, `_replicate_ownership`, `_iter_verify_tasks`, `_verify_*`, `_refuse_specials_before_copy`, ~500 lines across 1157-1233, 1454-1610, 1910-2160, 3055-3075) is reachable only via `copy_tree(source_fd=None)`/`verify_copy(source_fd=None)`; grep shows no production caller, and the two implementations drift (envelope-then-publish vs publish-then-populate, parallel vs serial hashing). Decide: delete the legacy path and require `source_fd`, or keep it as the documented library API. |
| rf-perf-70 | open | medium | medium | relocate_folder.py:1678 — [performance] In the runtime verify path `_pinned_kind_verify_task` hashes the source synchronously inside the `_run_streamed` producer, so only destination hashing reaches the pool and `--jobs` ("pool width for ownership + verify") has no effect (measured on 12 × 128 MiB cached files: 0.78 s at `-j1` vs 0.77 s at `-j8`, while legacy `_iter_verify_tasks` scales 1.43 s → 0.25 s). Dup the walker's `directory_fd` or open under the pinned stat check in the producer and move the digest into the pool task. |
| rf-dep-70 | open | medium | low | relocate_folder.py:2386 — [dependability] RENAME_NOREPLACE support is probed on the destination before the copy (`owner.create()`), but the source parent filesystem is first exercised in `_backup_target`/`_create_symlink` at swap time after the full copy, fsync walk, and verify; on NFS (EINVAL on rename flags) or FUSE without RENAME2, `_rename_noreplace` fails only then and the operator is told to finish by hand (traced). Add a mkdtemp + `_rename_noreplace` + rmdir probe in `source.parent` to `_execute_preamble`. |
| rf-val-70 | open | low | low | relocate_folder.py:277 — [input validation / command safety] The root-basename guard catches only an empty `src.name`; `Path("..").absolute().name` is `..`, so `relocate_folder.py .. ../../dest --dry-run` computes `target=<dest>/..` and reports it would migrate into the destination's parent with exit 0 (reproduced); a real run stops at `_path_taken` with a misleading "target already exists", and `--recover` derives `..relocate-backup`. Reject `..` and normalise via `resolve` in `Plan.from_args` and `_run_recover`. |
| rf-obs-70 | open | low | low | relocate_folder.py:617 — [observability / operability] Without `/proc` (macOS, which the POSIX capability gate at 3316 admits) `find_open_file_holders` returns an empty snapshot and `_check_no_open_files` passes silently, while the README promises the open-file precheck unless `--force`; no log line distinguishes "no holders" from "check unavailable" (traced). Warn or flag the snapshot when the precheck is skipped. |
| rf-sec-70 | open | low | low | relocate_folder.py:929 — [security] STRIDE Denial of service: `_open_pinned_file` compares `fstat` after the open, but `os.open(name, O_RDONLY\|O_NOFOLLOW, dir_fd=...)` has no `O_NONBLOCK`, so a same-name FIFO substituted between the walk stat and the open blocks forever and the stall watchdog only warns (reproduced: no return within 3 s after `os.mkfifo`). Open with `O_NONBLOCK`, run the existing `S_ISREG` check, then clear the flag via `fcntl`. |
| rdv3-cx-61 | open | low | low | remove-deduplv3.py:226 — [code complexity] Split _emit_remove_commands; Complexipy measures cognitive complexity 17, above the repository limit of 10. Preserve survivor selection, case warnings, and removal output. |
| rdv3-di-60 | open | high | medium | remove-deduplv3.py:234 — [data integrity] Collapse or conservatively reject path aliases before selecting a survivor; records for `file.txt` and `./file.txt` currently promise to save `file.txt` while emitting `rm -f -- ./file.txt`, deleting that same directory entry. Cover normalized relative/absolute spellings and directory aliases without conflating distinct hardlink entries. |
| rdv3-di-70 | open | high | medium | remove-deduplv3.py:243 — [data integrity] `_survivor` nominates the producer's `+N more` alias-cap sentinel (hash-recursive-ai5.py:1314) as the survivor because its 7-char basename beats shorter real names; reproduced with 4 hardlinks + 1 copy under `--alias-cap 2`: `# saving: +2 more` then `rm -f --` of every real path including the distinct inode. Never nominate a sentinel; skip non-path records (see dnp-di-70). |
| rdv3-sec-60 | open | high | low | remove-deduplv3.py:247 — [security] Encode filenames as single-line text in saving and case-conflict comments; decoded newline-bearing paths currently escape the `#` comment and inject shell commands despite quoting removal operands. STRIDE Tampering / Elevation of privilege; safe reproduction with `rm` stubbed executed a `printf AUDIT_MARKER` filename suffix through both line 247 and `_emit_case_variant_warning` line 222. Add shell-execution regression coverage for both comment paths. |
| rdv3-rel-70 | open | medium | low | remove-deduplv3.py:247-249 — [reliability / correctness] A tagged JSON path decoding to a lone surrogate the active stdout error handler cannot encode (`\udcff` under `--strict`, `\ud800` under default `surrogateescape`) dies with an uncaught `UnicodeEncodeError`, exit 1 instead of the documented 3, after a partial script is on stdout. Try-encode in `_decode_record_path` and count as skipped; `tests/test_remove_deduplv3.py:112` covers only raw bytes. |
| rdv3-val-70 | open | medium | low | remove-deduplv3.py:81 — [input validation / command safety] `_decode_record_path` accepts a JSON payload containing `\u0000`; `shlex.quote` passes the NUL inside quotes and POSIX shells drop it, so the emitted command names a different file (reproduced: `"/d/ab\u0000"` ran `rm -f -- /d/ab` with `rm` stubbed). STRIDE Tampering via crafted hash file. Skip and count decoded paths containing NUL, mirroring dnp-val-60. |
| rf-test-70 | open | medium | medium | tests/test_relocate_folder.py:1 — [test coverage] The 5,223-line main suite makes 70 `copy_tree`/`verify_copy` calls and none pass `source_fd=`, so every copy/verify/space/progress/ownership/cleanup assertion exercises the legacy path the CLI never runs, while the runtime path has ~300 dedicated lines; rf-rel-70, rf-rob-70, and rf-perf-70 are exactly the behaviours the legacy tests assert correctly for the dead path (e.g. `test_src_size_totals_sparse_alloc_under_apparent` at 4234). Port the copy/verify tests to drive `execute`/`copy_tree(source_fd=...)` and add a subprocess SIGKILL test. |

## Blocked / Deferred

| id | status | severity | effort | description |
|---|---|---|---|---|
| ie-arch-50 | blocked | low | high | import_events.py:1464-4142 — [architecture/modularity] Migrate model lifecycle and extraction behind OmniTensor's planned accelerator-only `event-extraction` workload only after that external workload proves file-format, privacy, partial-result, JSON/ICS, offline, and exit-code parity. |
| rf-test-51 | blocked | low | low | tests/test_relocate_folder.py:1181-1190 — [test/reliability] Decide whether `_check_cross_device` must remain controllable through a global `Path.stat` monkeypatch or the legacy test should use the documented `stat_fn` injection seam; both expectations cannot hold under the current implementation, and the legacy test fails the full suite. |

## Rejected / Won't fix

| id | status | severity | effort | description |
|---|---|---|---|---|
| bt-dead-50 | wont_fix | low | — | bookmark-tidy.py:1754-1758 — Keep the private helper's defensive missing-model guard; direct callers get a typed `UserError` even though the production caller already guards it. |
| dnp-i18n-01 | wont_fix | low | — | dedupl_numpy.py:21-30,228-259 — No translation catalog for this developer CLI unless localization becomes a product requirement; hard-coded English is accepted. |
| dnv3-i18n-01 | wont_fix | low | — | deduplicate-by-namev3.py:54-62,162-261 — No translation catalog for this developer CLI unless localization becomes a product requirement; hard-coded English is accepted. |
| hr-plat-05 | rejected | low | — | hash-recursive-ai5.py:2028-2041 — Text-mode hash-dump offsets remain valid on Windows because `tell()`/`seek()` use compatible cookies and the patched fixed-width digest contains no newline. |
| ie-obs-51 | rejected | low | — | import_events.py:1000-1022 — `_redirect_stdout_stderr` does not swallow other-worker logs because startup logging owns a duplicated real stderr descriptor. |
| ie-mem-01 | rejected | medium | — | import_events.py:32 — Keep native/default LLM context sizing (`0`) per explicit product decision; users can lower memory with `--llm-context`. |
| ie-api-01 | rejected | low | — | import_events.py:671-730 — Keep direct `ModelConfig` custom-path/default-pin behavior; the public CLI factory already clears default pins, and hand-constructed mismatched configs are not a user flow. |
| lq-plat-02 | rejected | low | — | link_queue.py:209-234 — Keep inherited Windows profile ACL behavior; it supplies the write-integrity goal that POSIX mode `0700` provides without adding a `pywin32`/`icacls` dependency. |
| lq-plat-03 | rejected | low | — | link_queue.py:2485-2533 — Keep `start_new_session=True`; CPython accepts and ignores it on Windows, while process-tree shutdown uses `taskkill /T`. |
| mkp-i18n-01 | wont_fix | low | — | minikeypad.py:1045-1510 — No GUI translation catalog unless localization becomes a product requirement; hard-coded English is accepted. |
| oze-unused-50 | wont_fix | low | — | organize_by_extension.py:749-756 — Keep `list_files` as the explicit compatibility wrapper; tests and external importers may rely on its list-returning public surface while runtime uses streaming `_iter_files`. |
| rdv3-plat-01 | rejected | low | — | remove-deduplv3.py:1-24 — POSIX-shell output is the utility's explicit deliverable; adding Windows command dialects would expand scope and review risk. |
